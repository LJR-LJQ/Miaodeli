function onClickHeaderSwitchButton(event) {
	//switchHeader();
}

function onClickParentDir(event) {
	//alert(this.textContent);
	gotoDir(this._dataObj.path);
}

function onClickSubDir(event) {
	//alert(this.textContent);
	gotoDir(this._dataObj.path);
}

function onClickFile(event) {
	//alert(this.textContent);
}